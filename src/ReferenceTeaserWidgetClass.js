import * as Scrivito from 'scrivito';

const ReferenceTeaserWidget = Scrivito.provideWidgetClass('ReferenceTeaserWidget', {
    attributes: {
        page: 'reference',
    },
});

export default ReferenceTeaserWidget;